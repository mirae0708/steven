// 예시: obsidian 폴더의 md 파일을 불러와 표시 (실제 구현은 서버/로컬 환경에 따라 다름)
document.addEventListener('DOMContentLoaded', () => {
  const content = document.getElementById('content');
  content.innerHTML = '<p>Obsidian, Wiki, 클라우드 연동 구조가 준비되었습니다.<br>실제 마크다운 파일 연동은 추가 개발이 필요합니다.</p>';
});